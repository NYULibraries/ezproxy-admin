<%= form_for(@resource) do |f| %>
  <% if @resource.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@resource.errors.count, "error") %> prohibited this resource from being saved:</h2>

      <ul>
      <% @resource.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <% @resource.attributes.keys.each do |field| %>
    <% unless field == "_id" %>
      <div class="field">
        <%= f.label field %><br />
        <%= f.text_field field %>
      </div>
    <% end %>
  <% end %>

  <div class="field">
    <label for="resource_column_name">Other</label><br />
    <input id="resource_column_name" name="resource[column_name]" size="5" />
    <input id="resource_column_value" name="resource[column_value]" size="19" />
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
